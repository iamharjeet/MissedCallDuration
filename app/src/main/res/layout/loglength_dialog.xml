<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/colorPrimary"
    android:minWidth="@dimen/dialog_min_width"
    android:orientation="vertical">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:minHeight="50dp"
        android:paddingLeft="@dimen/dialog_title_padding"
        android:paddingRight="@dimen/dialog_title_padding"
        android:text="@string/loglength_dialog_title"
        android:textColor="@color/text_color_white"
        android:textSize="@dimen/dialog_title_text" />

    <View
        android:layout_width="fill_parent"
        android:layout_height="1dp"
        android:layout_marginTop="0dp"
        android:background="@android:color/darker_gray" />

    <ListView
        android:id="@+id/loglength_list"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@color/listItemBackgroundColor"
        android:divider="@color/dividerColor"
        android:dividerHeight="1dp" />

</LinearLayout>